version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # - echo "DATABASE_URL=$DATABASE_URL" >> .env
        - echo "DATABASE_URL=$DATABASE_URL" >> .env
        - cat prisma/.env
        - npm run build
        - npm run generate
        # - npm run migrate
        - prisma db pull
        # - npm run seed
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
